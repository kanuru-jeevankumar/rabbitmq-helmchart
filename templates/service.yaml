apiVersion: v1
kind: Service
metadata:
  name: {{ include "rabbitmq-jeevan.fullname" . }}
  labels:
    {{- include "rabbitmq-jeevan.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "rabbitmq-jeevan.selectorLabels" . | nindent 4 }}
