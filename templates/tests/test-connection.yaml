apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "rabbitmq-jeevan.fullname" . }}-test-connection"
  labels:
    {{- include "rabbitmq-jeevan.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "rabbitmq-jeevan.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
